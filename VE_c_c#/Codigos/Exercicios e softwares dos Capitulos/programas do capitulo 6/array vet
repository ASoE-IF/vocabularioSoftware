#define DIM 5
#include <stdio.h>
void main(){
int vetor[DIM];
unsigned int i, num;
printf ("Entre com o numero inicial do conjunto. ");
scanf ("%d", &num);
for (i = 0 ; i < DIM; i++) vetor[i] = num++;
for (i = 0; i < DIM; i++)
printf ("Elemento %d = %d\n", i, vetor[i]);
}