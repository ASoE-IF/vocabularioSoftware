#define DIML 5
#define DIMC 40
#include <stdio.h>
void main(){
int i, j;
int nomes[DIML][DIMC];
for (i = 0; i < DIML; i++){
printf (“Entre com a linha %d”, i);
scanf (“%s”, nomes[i]);
}
for (i = 0; i < DIML; i++){
printf(“O nome %d e\n”, i);
printf (nomes[i]);
}
}