#include<stdio.h>
#include<stdlib.h>

void main(void)
{
	FILE *arquivo;
	char digite[100];
	int i;
	arquivo = fopen("arquivo de diretorio.txt", "w");
	gets(digite);
	for (i = 0; digite[i]; i++)
		putc(digite[i], arquivo);	/* Grava a string, caractere a caractere */
	fclose(arquivo);
	return 0;
}