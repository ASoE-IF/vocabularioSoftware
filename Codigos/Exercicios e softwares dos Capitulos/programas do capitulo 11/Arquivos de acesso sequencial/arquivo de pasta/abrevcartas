/* Lendo e imprimindo um arquivo seqüencial */
#include <stdio.h>
main()
{
FILE *impri_carta;
char imprime[50];
impri_carta = fopen("cartas.txt", "r");

while(fgets(imprime, 50, impri_carta)!=NULL)
{
	printf("%s", imprime);
		}
		fclose(impri_carta);
	return 0;
}