#include<stdio.h>

int main(void){
FILE *file;

file=fopen("cartas.txt", "r");
if(file==NULL){
printf("Arquivo não pode ser aberto.\n");
return 0;
}
int cha;

while((cha=fgetc(file))!=EOF)
putchar(cha);
fclose(file);
return 0;
}