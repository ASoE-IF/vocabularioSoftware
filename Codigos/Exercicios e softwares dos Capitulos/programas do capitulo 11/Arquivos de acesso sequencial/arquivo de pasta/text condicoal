#include<stdio.h>

int main(void)
{
	FILE *file;
	char x[50];
	file=fopen("cartas.txt", "r");
	if(file==NULL){
	printf("Arquio nao pode ser aberto.");
	return 0;
	}
	else{
	printf("arquivo aberto com sucesso.\n");
	while(fgets(x, 50, file)!=NULL){
	printf("%s", x);
	}
	fclose(file);
}
}