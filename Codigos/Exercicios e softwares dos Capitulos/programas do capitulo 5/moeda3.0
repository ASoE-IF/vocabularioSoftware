#include<stdio.h>
#include<time.h>
int aposta(int);
denovo()
{
	int y;
	printf("\nAposta inesistente.\nTente novamente");
	printf("1)Cara.\n2)Coroa.\nDigite: ");
	scanf("%d", &y);
	if (y > 2)
		printf("Aposta inesistente.\nTente novamente");
	if (y > 2)
		denovo();
	if (y < 1)
		printf("Aposta inesistente.\nTente novamente");
	if (y < 1)
		denovo();
}

int main()
{
	int saldo, x, y, z, creditos, a;
	saldo = 10;
	creditos = saldo;
	a = 1;
	printf("Você tem %d reais em saudo de aposta.\nSaldo de aposta deve ser menor ou igual 10\ne/ou maior ou igual a 1.\n", saldo);
	srand((unsigned)time(NULL));
	while (a <= 5)
	{
		a++;
		z = rand() % 2 + 1;
		printf("Digite o total a ser apostado: ");
		scanf("%d", &x);
		if (x > 10)
		{
			while (x > 10)
			{
				printf("\nImpossivel apostar.\nTente de novo.");
				printf("Digite o total a ser apostado: ");
				scanf("%d", &x);
			}
		}
		if (x < 10)
			while (x > 10)
			{
				printf("\nImpossivel apostar.\nTente de novo.");
				printf("Digite o total a ser apostado: ");
				scanf("%d", &x);

			}
		printf("1)Cara.\n2)Coroa.\nDigite: ");
		scanf("%d", &y);
		if (y < 1)
			denovo();
		if (y > 2)
			denovo();
		if (y == z)
		{
			printf("Você acertou.\n");
			creditos = creditos + x;
			if (creditos < 20)
			{
				printf("\ncontinue.\n");
				printf("\nVocê tem %d rais em saldo de aposta.\n", creditos);
			}
			if (creditos >= 20)
			{
				printf("\nVocê tem %d rais.\n", creditos);
			}
		}
		if (creditos < 1)
		{
			printf("\nVocê perdeu.\n");
			if(creditos<1)
			return 0;
		}
		if (creditos >= 20)
			printf("\nVocê venceu.\n");
			if(creditos>=20)
		return 0;
		}
				if (y != z)
		{
			printf("Você errou.");
			creditos = creditos - x;
			printf("\nVocê tem %d reais em saldo de aposta.\n", creditos);
		}
	}
