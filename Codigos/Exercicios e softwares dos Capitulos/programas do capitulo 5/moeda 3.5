#include<stdio.h>
#include<time.h>
int main()
{
	int saldo, x, y, z, creditos, a;
	saldo = 10;
	creditos = saldo;
	a = 1;
	printf
		("Você tem %d reais em saudo de aposta.\nSaldo de aposta deve ser menor ou igual 10\ne/ou maior ou igual a 1.\nValor maximo de aposta 5 reais.\n",
		 saldo);
	srand((unsigned)time(NULL));
	while (creditos != 20)
	{
		a++;
		x = rand() % 5 + 1;
		y = rand() % 2 + 1;
		z = rand() % 2 + 1;
		printf("Digite o total a ser apostado: %d", x);

		printf("\n1)Cara.\n2)Coroa.\nDigite: %d\n", y);
		if (y != z)
		{
			printf("Você errou.");
			creditos = creditos - x;

		}
		else
		{
			printf("Você acertou.\n");
			creditos = creditos + x;
			printf("\nVocê tem %d reais em saldo de aposta.\n", creditos);
		}
		if (creditos < 20)
		{
			printf("\ncontinue.\n");
			printf("\nVocê tem %d rais em saldo de aposta.\n", creditos);
		}
		if (creditos >= 20)
		{
			printf("\nVocê venceu.\n");
			printf("\nVocê tem %d reais.\n", creditos);
			printf("voce ganhou.\n");
			return 0;
		}
		if (creditos < 1)
		{
			printf("\nVocê perdeu.\n");
			printf("\nvoce tem %d", creditos);
			return 0;
		}
	}
}