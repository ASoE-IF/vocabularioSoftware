#include<stdio.h>
#include<time.h>
adivinhe()
{
	int x, y, a;
	a = 1;
	printf("Tentativa de numero (%d).\n", a);
	printf("Estou pensando em um numero entre um e 10.\nAdivinhe qual é.\n");
	printf("Digite uma tentativa: ");
	scanf("%d", &x);
	srand((unsigned)time(NULL));
	y = rand() % 10;
	if (x == y)
		printf("Parabéns.\nVocê acertou.\nQuer tentar novamente?\n\n");
	if (x == y)
		tente();
	if (x != y)
		printf("Você errou.\n");
	if (x > y)
		printf("O numero escolhido é muito grande.\nTente novamente.\n\n");
	if (x < y)
		printf("O numero escolhido é muito pequeno.\nTente novamente.\n\n");
	if (x != y)
		while (x != y)
		{
			a++;
			printf("Tentativa de numero (%d).\n", a);
			printf("Digite uma tentativa: ");
			scanf("%d", &x);
			if (x == y)
				printf
					("Parabéns.\nVocê acertou.\nQuer tentar novamente?\n\n");
			if (x == y)
				tente();
			if (x != y)
				printf("Você errou.\n");
			if (x > y)
				printf
					("O numero escolhido é muito grande.\nTente novamente.\n\n");
			if (x < y)
				printf
					("O numero escolhido é muito pequeno.\nTente novamente.\n\n");
		}
}

tente()
{
	int z;
	printf("1)Continue.\n2)Sair.\n");
	printf("Digite: ");
	scanf("%d", &z);
	if (z != 1)
		return 0;
	else
		adivinhe();
}

main()
{
	adivinhe();
}