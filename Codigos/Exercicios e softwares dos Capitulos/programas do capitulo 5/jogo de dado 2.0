#include<stdio.h>
#include<time.h>

void ladrilho(void)
{
printf("\t\t\t|***********************|\n");
}

void ladrilho2(void)
{
printf("\t\t|***************************************|\n");
}
int main(void)
{

	int dado1, dado2, jogo,  statos, ponto;
	
	srand((unsigned)time(NULL));
		ladrilho();
			printf("\t\t\t|Jogo de dados.         |\n");
		ladrilho();
			dado1 = rand() % 6 + 1;
			dado2 = rand() % 6 + 1;
	jogo = dado1 + dado2;
	switch (jogo)
	{
		case 7:
		case 11:
				ponto = dado1 + dado2;
				printf("\t\t\t|Ponto = %d + %d = %d\t|\n", dado1, dado2, ponto);
						ladrilho2();
						
				printf("\t\t|O jogado obteve %d + %d = %d.\t\t|\n", dado1, dado2, jogo);
						ladrilho2();
						
				printf("\t\t|O jogador venceu.\t\t\t|\n");
				printf("\t\t*****************************************");
							return 0;

		case 2:
		case 3:
		case 12:

				ponto = dado1 + dado2;
				printf("\t\t\t|Ponto = %d + %d = %d\t|\n", dado1, dado2, ponto);
						ladrilho2();
						
				printf("\t\t|O jogador obteve %d + %d = %d.\t\t|\n", dado1, dado2, jogo);
						ladrilho2();
						
				printf("\t\t|O jogador perdeu.\t\t\t|\n");
				printf("\t\t*****************************************");
							return 0;

	default:
			statos = 0;
			ponto = dado1 + dado2;
					printf("\t\t\t|Ponto = %d + %d = %d\t|\n", dado1, dado2, ponto);
							ladrilho2();
									break;
	}
	
	while (statos == 0)
	{
		dado1 = rand() % 6 + 1;
		dado2 = rand() % 6 + 1;
		jogo = dado1 + dado2;
				printf("\t\t|O jogador obteve %d + %d = %d.\t\t|\n", dado1, dado2, jogo);
				if ((jogo == 2) || (jogo == 3) || (jogo == 12))
				{
						ladrilho2();
								printf("\t\t|O jogador perdeu.\t\t\t|\n");
								printf("\t\t*****************************************");
											statos = 2;
					}

					else if (jogo == ponto)
							{
									ladrilho2();
											printf("\t\t|O jogador venceu.\t\t\t|\n");
											printf("\t\t*****************************************");
														statos = 1;
									}
					}
		return 0;
}