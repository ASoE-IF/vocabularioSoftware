#include<stdio.h>
#include<math.h>
int gomes(int, int);
main(){
int x, y, z;
printf("digite: ");
scanf("%d", &y);
printf("digite: ");
scanf("%d", &z);
x=gomes(y, z);
printf("\n%d", x);
}
int gomes(int xa, int xb){
return sqrt(xa+xb);
}